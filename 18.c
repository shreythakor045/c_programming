#include<stdio.h>
int main()
{
    int l,area,perimeter;
    printf("enter length:");
    scanf("%d",&l);
    area = l*l;
    perimeter = 4*l;
    printf("area = %d\n perimeter = %d\n", area , perimeter);
    return 0;
    
}